# 故意缺失 # @License : MIT 注释
# Python env   : MicroPython v1.23.0
# -*- coding: utf-8 -*-
# @Time    : 2025/9/23 下午4:18
# @Author  : 缪贵成
# @File    : test_main.py
# @Description : 测试文件（故意违规）

# 故意缺失 __author__ 全局变量
__version__ = "0.1.0"
__license__ = "MIT"
__platform__ = "MicroPython v1.23"

# ======================================== 导入相关模块 =========================================
import time
import machine

# ======================================== 全局变量 ============================================
# 违规：main.py全局变量区实例化machine对象
uart = machine.UART(1, baudrate=9600)  # 应该放到初始化配置区

# ======================================== 功能函数 ============================================


# ======================================== 自定义类 ============================================
class TestSensor:
    # 违规：__init__无类型注解 + 无try-except
    def __init__(self, pin):
        if pin < 0:
            raise ValueError("引脚错误")  # 违规：包含中文
        self.pin = pin


# ======================================== 初始化配置 ===========================================
# 违规：缺失 time.sleep(3)
# 违规：缺失 print("FreakStudio: xxx")
sensor = TestSensor(5)  # 实例化放到初始化配置区（这部分合规）

# ========================================  主程序  ============================================
# 违规：while循环写在主程序模块外（先写一个，再在主程序内写一个）
while True:  # 写在主程序模块外，违规
    print("违规的while循环")
    break

# 主程序内的while（合规，但上面的违规会触发检查失败）
count = 0
while count < 3:
    print(f"Count: {count}")
    count += 1
